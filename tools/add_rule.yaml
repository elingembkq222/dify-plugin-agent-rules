identity:
  name: add_rule
  author: dify
  label:
    en_US: Add Rule
    zh_Hans: 添加规则
  icon: icon.svg
description:
  human:
    en_US: Add or update a rule set in the rule engine
    zh_Hans: 在规则引擎中添加或更新规则集
  llm: Add or update a rule set in the rule engine

parameters:
  - name: rule_json
    type: object
    required: true
    label:
      en_US: Rule Set JSON
      zh_Hans: 规则集JSON
    form: llm
    human_description:
      en_US: Complete RuleSet JSON to add or update
      zh_Hans: 要添加或更新的完整规则集JSON

extra:
  python:
    source: tools/add_rule.py